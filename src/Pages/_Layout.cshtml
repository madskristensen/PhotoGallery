<!DOCTYPE html>
<html lang="en" itemscope itemtype="http://schema.org/ImageGallery">
<head prefix="og: http://ogp.me/ns#">
    <meta charset="utf-8" />
    <title itemprop="name">@ViewData["Title"] - PhotoGallery</title>
    <meta name="description" content="@ViewData["Description"]" />
    <meta name="keywords" content="@ViewData["Title"], photo, picture, gallery, images" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/site.scss" inline />
    @RenderSection("head", required: false)
    <meta name="application-name" content="@ViewData["Title"]" />
    <meta property="og:type" content="website" />
    <meta property="og:title" content="@ViewData["Title"]" />
    <meta property="og:url" content="@(Context.Request.Scheme + "://" + Context.Request.Host + Context.Request.Path)" />
    <meta property="og:image" content="@ViewData["Image"]" />
    <link rel="shortcut icon" href="~/img/favicon.png" type="image/png" />
</head>
<body>
    <header>
        <div class="container">
            <h1><a href="~/">PhotoGallery</a></h1>
        </div>
    </header>

    <main class="container">
        @RenderBody()
    </main>

    <footer>
        <p class="container">
            Fork me on <a href="https://github.com/madskristensen/PhotoGallery">GitHub</a>
            @{
                if (User.Identity.IsAuthenticated)
                {
                    <a href="~/login?logout=1">Sign out</a>
                }
                else
                {
                    <a asp-page="Login">Sign in</a>
                }
            }
        </p>
    </footer>

    <script src="/js/site.js" inline></script>
    @RenderSection("scripts", required: false)
</body>
</html>
